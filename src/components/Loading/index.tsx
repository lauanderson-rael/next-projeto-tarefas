import styles from './styles.module.css'

const Loading = () => {
    return (
        <div className={styles.loading}>
            <p>Carregando...</p>
        </div>
    );
}

export default Loading;
